org.gradle.jvmargs=-Dfile.encoding=UTF-8

# Fork-publishing branch: publish unsigned. signAllPublications() is gated on
# this property, so the Maven publication does not register GPG .asc artifacts
# that JitPack (and local publishToMavenLocal) cannot produce.
panamaPortSkipSigning=true
